# サイト内容の更新方法

公開ページの基本情報は `src/data/site.json` で管理しています。変更後は `node build.mjs` を実行すると、GitHub Pages用の `docs` が更新されます。

## REPの主要器具を追加する

1. 店舗で撮影した写真を `src/assets/uploads` に入れます。
2. `equipment.featured` に最大3件まで追加します。

```json
{
  "name": "確認済みの商品名",
  "model": "確認済みの型番",
  "image": "/assets/uploads/写真ファイル名.jpg",
  "feature": "器具の特徴",
  "reason": "Recipeが選んだ理由"
}
```

商品名・型番・写真が未確認の器具は追加しません。空欄のままなら公開ページにも表示されません。

## その他の設備を追加する

`equipment.additional` に、確認済みの商品名だけを追加します。

```json
"additional": [
  "商品名1",
  "商品名2"
]
```

1件以上登録すると「その他の設備を見る」が表示されます。

## Q&Aを追加する

`faq` に質問と回答を追加します。公開されるのは先頭から最大4件です。

```json
{
  "question": "質問文",
  "answer": "確認済みの回答"
}
```

桑名駅・西桑名駅からの行き方は、利用する駅、出口、目印、所要時間を確認してから追加してください。空の項目や「準備中」などの仮文言は追加しません。
